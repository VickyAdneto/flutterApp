## 1.1.0

- Added optional `cardId` parameter support and send it to the web flow as `card_id` when provided.

## 1.0.0

- Initial release of `bankco_flutter_sdk`.
- Added `BankcoSdkScreen` to load Bankco web flow in Flutter via WebView.
- Added optional UID token query parameter support.
- Added configurable back behavior for WebView history.
