# bankco_flutter_sdk example

This is a runnable host app that integrates `bankco_flutter_sdk`.

## Run

```bash
cd example
flutter pub get
flutter run
```

The button in this app simulates backend UID generation and opens:

`BankcoSdkScreen(url: '<integrator-url>', uid: uid)`
